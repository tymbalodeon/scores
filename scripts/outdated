#!/usr/bin/env zsh

outdated_scores=($(./scripts/get_outdated "$@"))
IFS=$'\n' outdated_scores=($(sort <<<"${outdated_scores[*]}"))
for file in "${outdated_scores[@]}"; do
    echo "${(C)file//-/ }"
done
